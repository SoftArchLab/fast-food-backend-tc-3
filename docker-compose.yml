services:
    # FastFood application service
  fastfood:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fastfood_app
    ports:
      - "8080:8080"
    environment:
        - ASPNETCORE_ENVIRONMENT=Development
        - connectionStrings__DefaultConnection=server=fastfood-aurora.cluster-czek4s2q63n2.us-east-1.rds.amazonaws.com;port=3306;database=FastFoodDb;user=root;password=fastfood123;
    depends_on: 
        - fastfood_db_mysql
    restart: unless-stopped
  # MySQL database service for FastFood application
  fastfood_db_mysql:
    image: mysql:8.0
    container_name: fastfood_db
    environment:
      MYSQL_ROOT_PASSWORD: fastfood@123
      MYSQL_DATABASE: FastFoodDb
    ports:
      - "3306:3306"
      - "3307:3307"
    volumes:
      - mysql_data:/var/lib/mysql
    restart: always 
volumes:
  mysql_data: